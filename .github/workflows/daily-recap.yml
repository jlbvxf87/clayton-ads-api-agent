name: daily-recap

# Runs once a day, ~6pm PT. Snapshots campaign state, evaluates rules, and
# DMs each whitelisted user an end-of-day recap.
#
# 01:00 UTC next day = 6pm PDT (Mar–Nov), 5pm PST (Nov–Mar).

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch: {}

concurrency:
  group: recap
  cancel-in-progress: false

jobs:
  recap:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Verify required secrets are set
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
          META_AD_ACCOUNT: ${{ secrets.META_AD_ACCOUNT }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          missing=""
          for v in ANTHROPIC_API_KEY META_ACCESS_TOKEN META_AD_ACCOUNT TELEGRAM_BOT_TOKEN SUPABASE_URL SUPABASE_SERVICE_ROLE_KEY; do
            value="${!v}"
            if [ -z "$value" ]; then
              missing="$missing $v"
            else
              echo "  + $v is set (${#value} chars)"
            fi
          done
          if [ -n "$missing" ]; then
            echo "::error::Missing required GitHub secrets:$missing"
            exit 1
          fi

      - run: npm ci
      - run: npm run recap
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
          META_AD_ACCOUNT: ${{ secrets.META_AD_ACCOUNT }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL || 'claude-opus-4-7' }}
          ACCOUNT_TZ: ${{ secrets.ACCOUNT_TZ || 'America/Los_Angeles' }}
          BRIEFING_CHAT_IDS: ${{ secrets.BRIEFING_CHAT_IDS }}
